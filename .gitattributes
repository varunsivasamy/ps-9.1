# Normalize text files in the repo; keep shell scripts LF so they run on Lambda
# and in CI regardless of the checkout platform.
* text=auto
*.sh text eol=lf
*.yaml text eol=lf
*.yml text eol=lf
*.py text eol=lf
