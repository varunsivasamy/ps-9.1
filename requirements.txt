fastapi
mangum
boto3
anthropic
pydantic
pytest
python-dotenv
uvicorn

# Test-only dependencies
httpx        # required by fastapi.testclient
moto[dynamodb]  # mock AWS for audit store tests

# Local demo only -- not used by the deployed Lambda. Lets scripts/demo_groq_check.py
# and AGENT_LLM_PROVIDER=groq exercise the pipeline while the Anthropic account
# has no API credits. Safe to skip in a production install.
groq
moto[server]  # standalone DynamoDB mock server for a real localhost demo run
