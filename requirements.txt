fastapi
mangum
boto3
groq
pydantic
pytest
python-dotenv
uvicorn

# Test-only dependencies
httpx        # required by fastapi.testclient
moto[dynamodb]  # mock AWS for audit store tests
moto[server]  # standalone DynamoDB mock server for a real localhost demo run
